name: AfterlifeOS Builder

on:
  workflow_dispatch:
    inputs:
      DEVICE:
        description: 'Device Codename (e.g., surya, raven)'
        required: true
      BUILD_TYPE:
        description: 'Build Type'
        required: true
        default: 'userdebug'
        type: choice
        options:
          - userdebug
          - user
          - eng
      BUILD_VARIANT:
        description: 'Build Variant'
        required: true
        default: 'Test'
        type: choice
        options:
          - Test
          - Release
      LOCAL_MANIFEST_URL:
        description: 'URL to Local Manifest XML'
        required: true
      CLEAN_BUILD:
        description: 'Request Full Clean Build (Admins Only)'
        required: false
        type: boolean
        default: false
      DIRTY_BUILD:
        description: 'Dirty Build (Skip manifest wipe)'
        required: false
        type: boolean
        default: false
      DISABLE_FSGEN:
        description: 'Disable FSGen'
        required: false
        type: boolean
        default: false
  
  repository_dispatch:
    types: [Telegram-Builder]

permissions:
  contents: write
  actions: read

# Global Environment Variables (Mapped from either Inputs or Client Payload)
env:
  DEVICE: ${{ inputs.DEVICE || github.event.client_payload.DEVICE }}
  BUILD_TYPE: ${{ inputs.BUILD_TYPE || github.event.client_payload.BUILD_TYPE }}
  BUILD_VARIANT: ${{ inputs.BUILD_VARIANT || github.event.client_payload.BUILD_VARIANT }}
  LOCAL_MANIFEST_URL: ${{ inputs.LOCAL_MANIFEST_URL || github.event.client_payload.LOCAL_MANIFEST_URL }}
  CLEAN_BUILD: ${{ inputs.CLEAN_BUILD || github.event.client_payload.CLEAN_BUILD }}
  DIRTY_BUILD: ${{ inputs.DIRTY_BUILD || github.event.client_payload.DIRTY_BUILD }}
  DISABLE_FSGEN: ${{ inputs.DISABLE_FSGEN || github.event.client_payload.DISABLE_FSGEN }}
  REQUESTER: ${{ github.event.client_payload.REQUESTER }} # Only exists in API trigger

jobs:
  check-limit:
    name: 🛡️ Gatekeeper (Check Queue & Quota)
    runs-on: ubuntu-latest
    outputs:
      is_admin: ${{ steps.check_role.outputs.is_admin }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check Queue Depth
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Checking global queue depth..."
          COUNT=$(gh run list --workflow afterlife_build.yml --status in_progress --json databaseId --limit 100 | jq length)
          echo "Current active/queued workflows: $COUNT"
          MAX_CONCURRENT=4
          
          if [[ "$COUNT" -gt "$MAX_CONCURRENT" ]]; then
            echo "::error::⛔ QUEUE FULL! There are already 3 builds waiting in line."
            echo "Please try again later when the queue clears."
            exit 1
          fi
          
          echo "✅ Queue check passed. System load is acceptable."

      - name: Check Role & Rate Limit
        id: check_role
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_TOPIC_ID: ${{ secrets.TELEGRAM_TOPIC_ID }}
        run: |
          # --- 1. IDENTIFY USER ROLE ---
          # Priority: Env REQUESTER (from Bot) > github.actor (Manual Web Trigger)
          if [[ -n "$REQUESTER" ]]; then
            ACTOR="$REQUESTER"
            echo "🤖 Bot Trigger Detected. Acting on behalf of: $ACTOR"
          else
            ACTOR="${{ github.actor }}"
            echo "👤 Manual Trigger Detected. User: $ACTOR"
          fi
          
          PERM=$(gh api repos/${{ github.repository }}/collaborators/$ACTOR/permission --jq .permission)
          echo "User permission: $PERM"

          if [[ "$PERM" == "admin" ]]; then
            IS_ADMIN="true"
            LIMIT=-1 # Unlimited
            echo "✅ Admin Access Detected. Unlimited Quota."
          else
            IS_ADMIN="false"
            LIMIT=5  # Daily Limit for normal users
            echo "👤 Standard User Access ($PERM). Quota: $LIMIT/day."
          fi
          
          echo "is_admin=$IS_ADMIN" >> $GITHUB_OUTPUT

          COUNTER_FILE=".github/workflow_counter.json"
          CURRENT_DATE=$(date +%Y-%m-%d)

          if [[ ! -f "$COUNTER_FILE" ]] || ! jq empty "$COUNTER_FILE" 2>/dev/null; then
            echo "{}" > "$COUNTER_FILE"
          fi

          TODAY_RUNS=$(jq -r --arg user "$ACTOR" --arg date "$CURRENT_DATE" \
            'if .[$user][$date] then .[$user][$date] else 0 end' "$COUNTER_FILE")

          if [[ ! "$TODAY_RUNS" =~ ^[0-9]+$ ]]; then TODAY_RUNS=0; fi

          echo "Usage for $ACTOR on $CURRENT_DATE: $TODAY_RUNS / $LIMIT"

          if [[ $LIMIT -ne -1 && "$TODAY_RUNS" -ge $LIMIT ]]; then
            echo "::error::User $ACTOR has reached the daily limit of $LIMIT builds."

            # Calculate time until reset (00:00 UTC)
            NEXT_RESET=$(date -d "tomorrow 00:00" +%s)
            NOW=$(date +%s)
            DIFF=$((NEXT_RESET - NOW))
            HOURS_LEFT=$((DIFF / 3600))
            MINS_LEFT=$(( (DIFF % 3600) / 60 ))

            # Send Telegram Notification for Quota Limit
            curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            -d message_thread_id="${TELEGRAM_TOPIC_ID}" \
            -d parse_mode="Markdown" \
            -d text="🚫 *Build Request Rejected*
            *Device:* \`$DEVICE\`
            *Type:* \`$BUILD_TYPE\`
            *Variant:* \`$BUILD_VARIANT\`
            *Dirty:* \`$DIRTY_BUILD\`
            *Clean:* \`$CLEAN_BUILD\`
            *User:* \`$ACTOR\`
            *Reason:* Daily Limit Reached ($TODAY_RUNS/$LIMIT)
            *Reset In:* ${HOURS_LEFT}h ${MINS_LEFT}m (00:00 UTC)" || true

            exit 1
          fi

          NEW_RUNS=$((TODAY_RUNS + 1))
          jq --arg user "$ACTOR" --arg date "$CURRENT_DATE" --argjson runs "$NEW_RUNS" \
            'if .[$user] then .[$user][$date] = $runs else .[$user] = {($date): $runs} end' "$COUNTER_FILE" > tmp.json && mv tmp.json "$COUNTER_FILE"

          # --- NOTIFY SUCCESSFUL QUEUE ---
          if [[ "$IS_ADMIN" == "true" ]]; then
            QUOTA_INFO="Unlimited ♾️"
          else
            REMAINING=$((LIMIT - NEW_RUNS))
            QUOTA_INFO="$NEW_RUNS / $LIMIT (Left: $REMAINING)"
          fi

          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            -d message_thread_id="${TELEGRAM_TOPIC_ID}" \
            -d parse_mode="Markdown" \
            -d text="✅ *Build Request Accepted*
            *Device:* \`$DEVICE\`
            *Type:* \`$BUILD_TYPE\`
            *Variant:* \`$BUILD_VARIANT\`
            *Dirty:* \`$DIRTY_BUILD\`
            *Clean:* \`$CLEAN_BUILD\`
            *User:* \`$ACTOR\`
            *Quota Usage:* $QUOTA_INFO" || true


      - name: Commit Counter Update
        run: |
          git config --global user.email "actions@github.com"
          git config --global user.name "GitHub Actions"
          git pull --rebase origin main || echo "No changes to pull"
          git add .github/workflow_counter.json
          git commit -m "Update build quota for ${{ env.REQUESTER || github.actor }}" || echo "Nothing to commit"
          git push

  build-on-vps:
    name: 🏗️ Build ROM (${{ inputs.DEVICE || github.event.client_payload.DEVICE }})
    needs: check-limit
    runs-on: self-hosted
    concurrency:
      group: shared-vps-builder
      cancel-in-progress: false
    timeout-minutes: 480 
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Prepare Scripts
        run: chmod +x builder/*.sh

      - name: Sync Source
        run: |
          echo "CURRENT_STAGE=Syncing Source" >> $GITHUB_ENV
          echo ">>> Starting Sync on VPS..."
          bash builder/sync.sh

      - name: Disable FSGen (Pre-Build)
        if: env.DISABLE_FSGEN == 'true'
        run: |
          source builder/config.sh
          TARGET_FILE="$ROOTDIR/build/soong/fsgen/Android.bp"
          echo "Targeting: $TARGET_FILE"
          
          if [ -f "$TARGET_FILE" ]; then
            echo "⚠️ Backing up and patching FSGen..."
            cp "$TARGET_FILE" "$GITHUB_WORKSPACE/Android.bp.bak"
            
            # Inject 'enabled: false,' after the name definition
            sed -i '/name: "soong_filesystem_creator",/a \    enabled: false,' "$TARGET_FILE"
            
            echo "✅ Patch applied:"
            grep -A 2 "soong_filesystem_creator" "$TARGET_FILE"
          else
            echo "Target file not found. Skipping patch."
          fi

      - name: Build ROM
        env:
          IS_ADMIN: ${{ needs.check-limit.outputs.is_admin }}
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_TOPIC_ID: ${{ secrets.TELEGRAM_TOPIC_ID }}
        run: |
          echo "CURRENT_STAGE=Building ROM" >> $GITHUB_ENV
          echo ">>> Starting Build for ${{ env.DEVICE }}..."
          echo "Admin Status: $IS_ADMIN"
          bash builder/build.sh

      - name: Restore FSGen (Post-Build)
        if: always() && env.DISABLE_FSGEN == 'true'
        run: |
          source builder/config.sh
          TARGET_FILE="$ROOTDIR/build/soong/fsgen/Android.bp"
          BACKUP_FILE="$GITHUB_WORKSPACE/Android.bp.bak"
          
          if [ -f "$BACKUP_FILE" ]; then
            echo "♻️ Restoring original Android.bp..."
            cp -f "$BACKUP_FILE" "$TARGET_FILE"
            rm "$BACKUP_FILE"
            echo "✅ Restoration complete."
          else
            echo "No backup file found to restore."
          fi

      # This part was previously severely broken
      - name: Handle Large Artifacts (Telegram/PixelDrain)
        if: success()
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_TOPIC_ID: ${{ secrets.TELEGRAM_TOPIC_ID }}
        run: |
          echo "CURRENT_STAGE=Uploading Artifacts" >> $GITHUB_ENV
          echo ">>> Uploading to external storage..."
          bash builder/smart_upload.sh ${{ env.DEVICE }}

      - name: Set Build Date
        if: success()
        run: echo "BUILD_DATE=$(date +'%Y%m%d')" >> $GITHUB_ENV

      # - name: Upload Artifact to GitHub
      #   if: success()
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: AfterlifeOS-${{ env.DEVICE }}-${{ env.BUILD_DATE }}-${{ env.BUILD_VARIANT }}
      #     # Only upload small files/info to GitHub Artifacts to save storage
      #     path: |
      #       source/out/target/product/${{ env.DEVICE }}/AfterlifeOS*.zip

      - name: 🚨 Notify Workflow Failure
        if: failure()
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_TOPIC_ID: ${{ secrets.TELEGRAM_TOPIC_ID }}
        run: |
          ACTOR="${{ env.REQUESTER }}"
          if [[ -z "$ACTOR" ]]; then ACTOR="${{ github.actor }}"; fi

          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            -d message_thread_id="${TELEGRAM_TOPIC_ID}" \
            -d parse_mode="Markdown" \
            -d text="❌ *Workflow FAILED*
            *Device:* \`${{ env.DEVICE }}\`
            *Type:* \`${{ env.BUILD_TYPE }}\`
            *Variant:* \`${{ env.BUILD_VARIANT }}\`
            *Dirty:* \`${{ env.DIRTY_BUILD }}\`
            *Clean:* \`${{ env.CLEAN_BUILD }}\`
            *User:* \`$ACTOR\`
            *Stage:* ${{ env.CURRENT_STAGE || 'Initialization/Setup' }}
            *Link:* [View Action Run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})" || true

      - name: Notify Workflow Cancellation
        if: cancelled()
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_TOPIC_ID: ${{ secrets.TELEGRAM_TOPIC_ID }}
        run: |
          ACTOR="${{ env.REQUESTER }}"
          if [[ -z "$ACTOR" ]]; then ACTOR="${{ github.actor }}"; fi

          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            -d message_thread_id="${TELEGRAM_TOPIC_ID}" \
            -d parse_mode="Markdown" \
            -d text="⚠️ *Workflow Cancelled*
            *Device:* \`${{ env.DEVICE }}\`
            *Type:* \`${{ env.BUILD_TYPE }}\`
            *Variant:* \`${{ env.BUILD_VARIANT }}\`
            *Dirty:* \`${{ env.DIRTY_BUILD }}\`
            *Clean:* \`${{ env.CLEAN_BUILD }}\`
            *User:* \`$ACTOR\`
            *Stage:* ${{ env.CURRENT_STAGE || 'Initialization' }}" || true